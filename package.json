{
  "name": "harumaki",
  "version": "1.0.0",
  "description": "全国位置情報・実績解除アプリ",
  "scripts": {
    "cap:sync": "npx cap sync android",
    "cap:build:debug": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor-community/background-geolocation": "^1.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
